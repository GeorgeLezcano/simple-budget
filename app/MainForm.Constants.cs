namespace App;

partial class MainForm
{
    private const string shellText = "Simple Budget";
    private const string iconName = "$this.Icon";
    private const string shellName = "MainForm";
    private const int shellWidth = 1200;
    private const int shellHeight = 800;
    private SizeF DefaultAutoScaleDimensions = new(8F, 20F);
}